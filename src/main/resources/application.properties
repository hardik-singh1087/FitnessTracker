spring.application.name=Fitness Tracker

# 1. Database Connection (MySQL)
# Change 'fitness_db' to your actual schema name
spring.datasource.url=jdbc:mysql://localhost:3306/fitness_db
spring.datasource.username=root
spring.datasource.password=hardiksingh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 2. JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Use the MySQL 8 dialect (works for MySQL 8.0 and above)
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect